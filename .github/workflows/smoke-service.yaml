name: smoke-service

on:
  push:
    branches: [master]
    paths:
      - 'services/smoke/**'

env:
  IMAGE: ghcr.io/${{ github.repository_owner }}/smoke-service

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set version
        id: version
        run: echo "tag=$(jq -r '.["smoke-service"]' versions.json)" >> $GITHUB_OUTPUT

      - name: Build and push image
        if: contains(github.event.commits[0].modified, 'services/smoke/src') || contains(github.event.commits[0].added, 'services/smoke/src')
        uses: docker/build-push-action@v5
        with:
          context: services/smoke
          push: true
          tags: ${{ env.IMAGE }}:${{ steps.version.outputs.tag }}

      - name: Regenerate migrate.yaml from changelog
        run: bash scripts/generate-migrate.sh smoke-service

      - name: Update deployment manifest image tag
        run: |
          sed -i "s|${{ env.IMAGE }}:.*|${{ env.IMAGE }}:${{ steps.version.outputs.tag }}|" deploy/smoke-service/deployment.yaml

      - name: Commit and push manifest changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add deploy/smoke-service/
          git diff --cached --quiet || git commit -m "deploy smoke-service:${{ steps.version.outputs.tag }}"
          git push
